ha:
  activerecord:
    errors:
      messages:
        record_invalid:
        restrict_dependent_destroy:
          has_one: "%{record}が存在しているため削除できません"
          has_many: "%{record}が存在しているため削除できません"
    models:
      task: タスク
    attributes:
      task:
        id: ID
        name: 名前
        description: 詳しい説明
        created_at: 作成日時
        updated_at: 更新日時